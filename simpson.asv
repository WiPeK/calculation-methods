function[w] = simpson(a, b, n, av)
    w = 0;
    h = (b - a) / n;
    
    xarr = [b];
    yarr = [b];
    xarr(1) = a;
    xarr(b) = b;
    
    for i=2:(b-1)
       xarr(i) = xarr(1) + (i-1)*h;
    end
    
    for i=1:b
       yarr(i) = horner(av, xarr(i)); 
    end
    
    simres = yarr(1);
    
    for i=2:(b-1)
        
    end
    
    simres = simres + yarr(b);
    w = (h / 3) * simres;
end